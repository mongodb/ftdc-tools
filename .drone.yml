---
kind: pipeline
name: ftdc-tools
type: kubernetes

platform:
  os: linux
  arch: amd64

steps:
- name: test
  image: python
  pull: if-not-exists
  commands:
  - curl -sSL https://raw.githubusercontent.com/python-poetry/poetry/master/get-poetry.py | python -
  - . $HOME/.poetry/env
  - poetry install
  - poetry run pytest src tests
  when:
    branch:
      - main
    event:
      - pull_request
      - push
- name: publish
  image: python
  pull: if-not-exists
  settings:
    namespace: devprod-tips
  environment:
    PYPI_TOKEN:
      from_secret: ftdc-tools-secrets
  commands:
  - curl -sSL https://raw.githubusercontent.com/python-poetry/poetry/master/get-poetry.py | python -
  - . $HOME/.poetry/env
  - poetry config pypi-token.pypi $PYPI_TOKEN
  - poetry publish --build
  when:
    branch:
      - main
    event:
      - pull_request
      - push